import React from "react";

const EventCard = () => {
  return (
    <main>
      <div>
        Shows brief info about an event — event title, date/time, location (if
        any), and a short description or tags.
      </div>
    </main>
  );
};

export default EventCard;
